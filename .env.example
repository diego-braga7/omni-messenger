# Configuração do Ambiente
# development: Modo de desenvolvimento com hot-reload e logs detalhados
# production: Modo de produção otimizado e seguro
NODE_ENV=development

# Target de Build do Docker
# development: Instala todas as dependências e roda em modo watch (start:dev)
# runner: Build otimizado multi-stage, apenas deps de prod, roda compilado (start:prod)
# Opções: development | runner
BUILD_TARGET=development

# Database (PostgreSQL)
DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=omni_messenger

# Message Queue (RabbitMQ)
# Para rodar localmente fora do docker, use localhost. Dentro do docker, use o nome do serviço (rabbitmq)
RABBITMQ_URI=amqp://user:password@rabbitmq:5672
RABBITMQ_DEFAULT_USER=user
RABBITMQ_DEFAULT_PASS=password

# Z-API (WhatsApp Gateway)
ZAPI_INSTANCE_ID=sua_instance_id
ZAPI_TOKEN=seu_token
ZAPI_CLIENT_TOKEN=seu_client_token

# Google Calendar Integration (OAuth2)
GOOGLE_CLIENT_ID=seu_client_id
GOOGLE_CLIENT_SECRET=seu_client_secret
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google-calendar/callback

# Configurações do Postgres (Container)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=omni_messenger
